# Base URL for the API
BASE_URL=http://localhost:80

# Authentication credentials
USERNAME=your_username
PASSWORD=your_password

# JWT token (will be captured from auth response)
# You can also set this manually if you already have a token
AUTH_JWT=

# Optional: IDs for testing
USER_ID=self
ROLE_ID=2
BLOG_ID=1
LINKTREE_ID=your_linktree_id
REFERRAL_CODE=your_referral_code
