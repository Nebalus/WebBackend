meta {
  name: Get User
  type: http
  seq: 2
}

get {
  url: {{protocol}}://{{host}}/ui/admin/users/:userid
  body: none
  auth: bearer
}

params:path {
  userid: self
}

auth:bearer {
  token: {{auth_jwt}}
}

vars:pre-request {
  userid: 1
}

settings {
  encodeUrl: true
  timeout: 0
}
