meta {
  name: Delete User
  type: http
  seq: 5
}

delete {
  url: {{protocol}}://{{host}}/ui/admin/users/:userid
  body: none
  auth: bearer
}

params:path {
  userid: 2
}

auth:bearer {
  token: {{auth_jwt}}
}

vars:pre-request {
  userid: 2
}

settings {
  encodeUrl: true
  timeout: 0
}
