meta {
  name: Get Role
  type: http
  seq: 5
}

get {
  url: {{protocol}}://{{host}}/ui/admin/roles/:roleId
  body: json
  auth: bearer
}

params:path {
  roleId: 6
}

auth:bearer {
  token: {{auth_jwt}}
}
