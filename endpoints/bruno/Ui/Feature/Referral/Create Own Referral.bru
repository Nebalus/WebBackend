meta {
  name: Create Own Referral
  type: http
  seq: 2
}

post {
  url: {{protocol}}://{{host}}/ui/users/:userId/services/referrals
  body: json
  auth: bearer
}

params:path {
  userId: self
}

headers {
  : 
}

auth:bearer {
  token: {{auth_jwt}}
}

body:json {
  {
    "label": "TEST",
    "url": "https://example.com"
  }
}
