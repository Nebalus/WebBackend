meta {
  name: Delete Other Referrals
  type: http
  seq: 2
}

delete {
  url: {{protocol}}://{{host}}/ui/user/:userid/services/referrals/:code
  body: none
  auth: bearer
}

params:path {
  userid: 
  code: 
}

auth:bearer {
  token: {{auth_jwt}}
}
