meta {
  name: GetAll Other Referrals
  type: http
  seq: 5
}

get {
  url: {{protocol}}://{{host}}/ui/users/:userid/services/referrals/all
  body: none
  auth: bearer
}

params:path {
  userid: 1
}

auth:bearer {
  token: {{auth_jwt}}
}
