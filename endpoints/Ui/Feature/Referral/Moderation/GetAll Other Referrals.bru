meta {
  name: GetAll Other Referrals
  type: http
  seq: 5
}

get {
  url: {{protocol}}://{{host}}/ui/user/:userid/services/referrals/all
  body: none
  auth: bearer
}

params:path {
  userid: 
}

auth:bearer {
  token: {{auth_jwt}}
}
