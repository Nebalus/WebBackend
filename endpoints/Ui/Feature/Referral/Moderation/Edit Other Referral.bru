meta {
  name: Edit Other Referral
  type: http
  seq: 1
}

put {
  url: {{protocol}}://{{host}}/ui/user/:userid/services/referrals/:code
  body: none
  auth: bearer
}

params:path {
  code: 
  userid: 
}

auth:bearer {
  token: {{auth_jwt}}
}
