meta {
  name: Get Other Referral
  type: http
  seq: 3
}

get {
  url: {{protocol}}://{{host}}/ui/user/:userid/services/referrals/:code
  body: none
  auth: bearer
}

params:path {
  userid: 
  code: K5w1rfqC
}

auth:bearer {
  token: {{auth_jwt}}
}
