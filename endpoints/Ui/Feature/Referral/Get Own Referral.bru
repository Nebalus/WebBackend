meta {
  name: Get Own Referral
  type: http
  seq: 6
}

get {
  url: {{protocol}}://{{host}}/ui/users/:userid/services/referrals/:code
  body: none
  auth: bearer
}

params:path {
  userid: self
  code: 3bPrIEto
}

auth:bearer {
  token: {{auth_jwt}}
}
