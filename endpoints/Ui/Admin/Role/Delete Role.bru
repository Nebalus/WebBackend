meta {
  name: Delete Role
  type: http
  seq: 3
}

delete {
  url: {{protocol}}://{{host}}/ui/admin/roles/:roleId
  body: none
  auth: bearer
}

params:path {
  roleId: 5
}

auth:bearer {
  token: {{auth_jwt}}
}
