meta {
  name: Delete Role Permissions
  type: http
  seq: 2
}

delete {
  url: {{protocol}}://{{host}}/ui/admin/roles/:roleId/permissions
  body: json
  auth: bearer
}

params:path {
  roleId: 4
}

auth:bearer {
  token: {{auth_jwt}}
}

body:json {
  [
    "feature"
  ]
}
