meta {
  name: Delete Role Permissions
  type: http
  seq: 2
}

delete {
  url: {{protocol}}://{{host}}/ui/admin/role/:roleId/permission
  body: json
  auth: apikey
}

params:path {
  roleId: 4
}

auth:apikey {
  key: Authorization
  value: {{auth_jwt}}
  placement: header
}

body:json {
  [
    "feature"
  ]
}
