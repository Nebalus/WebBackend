meta {
  name: GetAll Roles from User
  type: http
  seq: 1
}

get {
  url: {{protocol}}://{{host}}/ui/admin/user/:userid/all
  body: none
  auth: inherit
}

params:path {
  userid: self
}

vars:pre-request {
  userid: 1
}

settings {
  encodeUrl: true
  timeout: 0
}
