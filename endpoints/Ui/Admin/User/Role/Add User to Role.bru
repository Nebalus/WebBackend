meta {
  name: Add User to Role
  type: http
  seq: 1
}

post {
  url: {{protocol}}://{{host}}/ui/admin/user/:userid/role/:roleid
  body: none
  auth: bearer
}

params:path {
  userid: self
  roleid: 2
}

auth:bearer {
  token: {{auth_jwt}}
}

settings {
  encodeUrl: true
  timeout: 0
}
