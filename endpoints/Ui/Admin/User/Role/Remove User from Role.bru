meta {
  name: Remove User from Role
  type: http
  seq: 1
}

delete {
  url: {{protocol}}://{{host}}/ui/admin/users/:userid/roles/:roleid
  body: none
  auth: bearer
}

params:path {
  roleid: 3
  userid: self
}

auth:bearer {
  token: {{auth_jwt}}
}

settings {
  encodeUrl: true
  timeout: 0
}
